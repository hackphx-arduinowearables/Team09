#include "Pods-OpenBLE.xcconfig"
GCC_PREPROCESSOR_DEFINITIONS = COCOAPODS=1
HEADER_SEARCH_PATHS = "${PODS_ROOT}/BuildHeaders" "${PODS_ROOT}/BuildHeaders/OpenBLE" "${PODS_ROOT}/Headers" "${PODS_ROOT}/Headers/OpenBLE"
OTHER_LDFLAGS = -ObjC ${PODS_OPENBLE_OTHER_LDFLAGS}
PODS_ROOT = ${SRCROOT}